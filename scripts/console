#!/usr/bin/nodejs

// Dont make this a var, it won't be exported to the REPL
server = require("../main.js")

console.log("starting REPL".green)
var repl = require("repl")
var context = repl.start("> ").context
context.util = require("util")